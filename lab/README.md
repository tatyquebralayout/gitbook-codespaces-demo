# Lab Instructions

The lab demonstrates a tiny data analysis task implemented in pure Python. Run the script and observe the output right in the Codespace.

```bash
python analyze.py
```

Feel free to modify the data in `analyze.py` and re-run the script to see different results.
